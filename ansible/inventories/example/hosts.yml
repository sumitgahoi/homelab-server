---
# Copy to inventories/production/ and fill secrets (use ansible-vault).
all:
  children:
    proxmox_api:
      hosts:
        localhost:
          ansible_connection: local
    vyos_gateway:
      hosts:
        vyos:
          # Override in group_vars or -e ansible_host=... (must match reachable VyOS LAN address)
          ansible_host: 192.168.1.1
