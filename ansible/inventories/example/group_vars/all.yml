---
# Site-specific overrides (most defaults live in roles/vyos_proxmox/defaults/main.yml)

proxmox_api_host: "pve.example.com"
proxmox_api_password: ""

vyos_lan_ipv4: "192.168.1.1"
vyos_ssh_user: "vyos"

# Optional: SSH target if it differs from vyos_lan_ipv4 (jump host / dual-stack)
# vyos_ansible_host: "192.168.1.1"
