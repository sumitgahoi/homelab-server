# WireGuard

Authority is this directory, not this file:

- Design: `wireguard/README.md`
- Apply: `wireguard/wg0.md`, `wireguard/wg1.md`, `wireguard/wg2.md`

Invariants: `REQUIREMENTS.md`. WAN IPv6 on `eth1` is CURRENT (`vyos/setup.md`). WAN cutover is complete. WireGuard apply is still a separate sitting.
