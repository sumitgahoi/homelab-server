# WireGuard

Authority is this directory, not this file:

- Design: `wireguard/README.md`
- Apply (`wg0`/`wg1`, `wg-india`): `wireguard/setup.md`

Invariants: `REQUIREMENTS.md`. WAN IPv6 on `eth1` is CURRENT (`vyos/setup.md`). WAN cutover is complete. WireGuard apply is still a separate sitting (`wireguard/setup.md`).
